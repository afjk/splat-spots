---
import "../styles/global.css";
import { href, absolute } from "../lib/url";

interface Props {
  title: string;
  description?: string;
  path: string;
  image?: string;
}

const TAGLINE =
  "Community-curated directory of publicly shared Insta360 Spatial Captures";
// `image` and `path` are app-absolute ("/og.png"); absolute() applies the
// deployment base exactly once. Do not pre-apply href() here.
const {
  title,
  description = TAGLINE,
  path,
  image = "/og.png",
} = Astro.props;

const fullTitle = title === "Splat Spots" ? `Splat Spots — ${TAGLINE}` : `${title} — Splat Spots`;
const canonical = absolute(path);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="dark" />
    <meta name="theme-color" content="#08080a" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href={href("/favicon.png")} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Splat Spots" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={absolute(image)} />
    <meta name="twitter:card" content="summary_large_image" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap"
    />
  </head>
  <body>
    <header class="site-header">
      <div class="shell">
        <a class="wordmark" href={href("/")}>
          <span class="wordmark-dots" aria-hidden="true"><i></i><i></i><i></i><i></i></span>
          Splat Spots
        </a>
        <nav class="site-nav">
          <a href={href("/")}>Gallery</a>
          <a href={href("/submit")}>Add a spot</a>
          <a class="optional" href="https://github.com/afjk/insta360-sog-xr-viewer" target="_blank" rel="noreferrer">Viewer ↗</a>
          <a href="https://github.com/afjk/splat-spots" target="_blank" rel="noreferrer">GitHub ↗</a>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="shell">
        <p class="disclaimer">
          一般公開された Spatial Capture のリンクを持ち寄る<strong>非公式のディレクトリ</strong>です。
          Insta360 および Arashi Vision とは関係がなく、承認も受けていません。Capture 本体は
          それぞれの提供元が配信しています。<br />
          Unofficial and not affiliated with Insta360. If you are the creator of a listed
          capture, <a href={href("/report")}>request removal</a> and it comes down.
        </p>
        <nav>
          <a href="https://github.com/afjk/splat-spots" target="_blank" rel="noreferrer">Source</a>
          <a href={href("/report")}>削除・修正依頼</a>
        </nav>
      </div>
    </footer>
  </body>
</html>
